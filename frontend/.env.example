# Base URL of the ArtLens search API. Falls back to http://localhost:8000
# when unset. For a container build, pass this as a build ARG instead
# (see Dockerfile) since Vite inlines env vars at build time.
VITE_API_URL=http://localhost:8000
